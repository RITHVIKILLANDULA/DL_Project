# Python
__pycache__/
*.pyc
*.pyo

# Virtual environment
.venv/
.env

# Notebooks
.ipynb_checkpoints/

# Claude Code agent state
.claude/

# Trained model checkpoints (only the DistilBERT safetensors is too large for git;
# small RNN/LSTM/baseline checkpoints are committed. DistilBERT goes in a GitHub Release).
models/best_transformer/model.safetensors
models/best_transformer_subsample_backup/

# GloVe embeddings (~330 MB; auto-downloaded by src/fake_news/embeddings.py)
data/raw/glove_embeddings/
data/raw/glove.6B.zip

# Training logs (verbose, regenerate by retraining)
reports/*.log

# OS / IDE
.DS_Store
Thumbs.db

# Temp dirs
.tmp_fakenewsnet/
tmp_fakenewsnet_old/
